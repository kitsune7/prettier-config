# `@happikitsune/prettier-config`

> My personal [Prettier](https://prettier.io) config.

## Usage

**Install**:

```bash
npm install --save-dev @happikitsune/prettier-config
```

**Edit `package.json`**:

```jsonc
{
  // ...
  "prettier": "@happikitsune/prettier-config"
}
```
